import 'package:flutter/material.dart';

class ArchieveScreen extends StatelessWidget {
  const ArchieveScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('archieve Screen');
  }
}
